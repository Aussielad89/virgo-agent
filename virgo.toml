[model]
planner = "ornith:latest"
generator = "ornith:latest"
fixer = "ornith:latest"
timeout = 300

[sandbox]
mode = "allowlist"
allowed_commands = [
    "python", "pip", "git", "ls", "cat", "echo",
    "pwd", "head", "tail", "wc", "sort", "grep",
    "find", "mkdir", "cp", "mv", "which",
]

[plugins]
watch = true
dirs = ["plugins", "~/.virgo/plugins"]

[chat]
model = "ornith:latest"
max_tokens = 2048
